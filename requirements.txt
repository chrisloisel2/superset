# Drivers Hive / Hadoop
pyhive[hive]==0.7.0
thrift==0.16.0
thrift-sasl==0.4.3
sasl==0.3.1

# Driver PostgreSQL (meta-DB Superset)
psycopg2-binary==2.9.9

# Driver MongoDB (dialect SQLAlchemy pour Superset)
pymongo==4.6.1
mongomock==4.1.2

# Cachelib
cachelib==0.10.2
